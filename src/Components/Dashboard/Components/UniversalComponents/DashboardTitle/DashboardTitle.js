import React from 'react';

// STYLED COMPONENTS
import {} from './DashboardTitleStyles';

const DashboardTitle = (props) => {
    return(
        <h1>{props.role} Dashboard</h1>
    )
}

export default DashboardTitle;